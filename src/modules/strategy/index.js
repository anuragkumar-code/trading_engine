const routes = require('./routes/strategy.routes');
const StrategyController = require('./controller/strategy.controller');
const StrategyService = require('./service/strategy.service');

module.exports = {
  routes,
  StrategyController,
  StrategyService,
};